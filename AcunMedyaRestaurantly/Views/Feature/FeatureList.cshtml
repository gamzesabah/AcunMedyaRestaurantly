﻿@model List<AcunMedyaRestaurantly.Entities.Feature>
@{
    ViewBag.Title = "FeatureList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    var count = 0;
}


<div class="row page-titles mx-0">
    <div class="col p-md-0">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0)">Anasayfa</a></li>
            <li class="breadcrumb-item active"><a href="javascript:void(0)">İçerik Bilgisi</a></li>
        </ol>
    </div>
</div>
<!-- row -->


<div class="container-fluid">
    <div class="row">

        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <div class="card-title">
                        <h4>İçerik Bilgisi</h4>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Alt Başlık</th>
                                    <th>Başlık</th>
                                    <th>Video</th>
                                    <th>Resim</th>
                                    <th>Sil</th>
                                    <th>Güncelle</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr>
                                        <td>@count</td>
                                        <td>@item.Subtitle</td>
                                        <td>@item.Title</td>
                                        <td>@item.VideoUrl</td>
                                        <td><img src="@item.ImageUrl" style="width:100px; height:100px;" /></td>

                                        <td>
                                            <a href="/Feature/FeatureDelete/@item.FeatureId" class="btn btn-info gradient-2">
                                                <i class="fa fa-close" color-muted m-r-5></i>
                                                Sil
                                            </a>
                                        </td>
                                        <td>
                                            <a href="/Feature/FeatureEdit/@item.FeatureId" class="btn btn-info gradient-6">
                                                <i class="fa fa-edit" color-muted m-r-5></i>
                                                Güncelle
                                            </a>
                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                        <a href="/Feature/FeatureCreate" class="btn btn-outline-success">Yeni İçerik Bilgisi Ekle</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>